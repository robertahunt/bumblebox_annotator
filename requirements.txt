# GUI Framework
PyQt6>=6.6.0
pyqtgraph>=0.13.3

# Computer Vision
opencv-python-headless>=4.8.0  # Headless version to avoid Qt conflicts
numpy>=1.24.0
Pillow>=10.0.0

# SAM2
torch>=2.0.0,<2.5.0
torchvision>=0.15.0,<0.20.0
# SAM2 must be installed separately: pip install git+https://github.com/facebookresearch/segment-anything-2.git

# Deep Learning Training
ultralytics>=8.0.0,<9.0.0  # For YOLO
transformers>=4.35.0,<5.0.0  # For Mask2Former
# detectron2 must be installed separately if needed: pip install 'git+https://github.com/facebookresearch/detectron2.git'
albumentations>=1.3.0,<2.0.0

# Data Management
pyyaml>=6.0
tqdm>=4.65.0
scikit-image>=0.21.0

# Utilities
pycocotools>=2.0.7
matplotlib>=3.7.0
